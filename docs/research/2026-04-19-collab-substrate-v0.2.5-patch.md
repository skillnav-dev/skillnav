# 协同底座 v0.2.5 补丁 — L4 连接机制 + 协同模式分层

<!-- 2026-04-18 会话生成 / 不改 v0.2 parked 原档 -->

## 本补丁为什么存在

v0.2 parked 档案（2026-04-18）因 N=1 过拟合风险整体搁置，等 N=4 回看。但 2026-04-18 当日另一场元层对话活体暴露了 **v0.2 的两处结构性缺失**，证据是"当日对话"本身，不需要 3 次实测才能看到：

1. **L4 层间传棒机制缺失** — 7 块资产存在但互不引用，每次新任务从 0 重新发现
2. **对话模式分层缺失** — 所有任务用同一套协议，未区分实施/调研/决策/元层/学习/纯对话

按 `选型元底座-目的与原则.md §5.2` 的判别标准：
- **结构性决策** → 先验证后落地（L4 连接机制 → **placeholder**，不立即实现）
- **可迭代产物** → 先落地后验证（6 种模式 → **v1 已落地**，用中修）

所以本补丁是**半热半冷**：一条 placeholder、一条 v1。

## 关系图

```
v0.2 parked (2026-04-18)
    ├── 冷冻骨架（图 A / 图 B / 8 载体）→ 不动
    └── v0.2.5 补丁（本文档）
          ├── Patch-1：L4 连接机制（placeholder，等 N=2 验证）
          └── Patch-2：协同模式分层（v1 已落地，用中修）

→ N=2 实测：RAG 知识库选型调研
→ N=3 实测：跨栈组件任务
→ N=4 回看：v0.3 正式骨架
```

## Patch-1 · L4 连接机制（placeholder）

### 诊断的活体样本（2026-04-18 会话）

| 症状 | 具体表现 |
|---|---|
| **资产互不引用** | `~/Vault/知识库/_元认知.md` 不知道 `rag-sop/profiles` 存在；rag-sop 不引用 Vault 卡片；两个体系互相看不见 |
| **新任务缺资产盘点反射** | 用户问"要做知识库 UI" → AI 立即进方案推荐模式；没先问"已有资产清单"。违反全局 feedback `借力用户成熟体系` |
| **MEMORY 更像状态备忘不像资产地图** | 4 轮对话才把 7 块资产拼出来，热区索引导航性不够 |

### 候选连接机制（3 选，都是推演，待 N=2 验证）

**候选 A：开工反射（最轻）**
任何"开始做 X" 指令触发前，AI 必须先输出：
1. 任务性质（按协同模式分层识别）
2. 相关已有资产清单（Vault / docs / rag-sop / Skills / observation ≤ 5 项）
3. 借力判断（底盘级 / 轮子级 / 真空地带）
4. 用户 go/no-go

**候选 B：`/借力盘点` skill（显式调用）**
用户主动执行 `/借力盘点 <task>`，AI 按协议扫描资产并输出清单。

**候选 C：MEMORY 重构为"资产地图"（重）**
MEMORY.md 从项目状态备忘 → 资产目录（按场景/技术栈分类），自动召回。

### 不在本补丁中实现，原因

这是**结构性决策**：错了会污染所有任务起手。A3 反方警告成立。

**N=2 实测（RAG 知识库选型调研）时**，做一次"假装有反射"的演练：
- 用户说"开始选型调研" → AI 手动跑一遍反射动作
- 观察：发现资产是否充分？起手是否真的跳过"从 0 讨论"？用户体感是否改进？
- 产出：`docs/research/collab-substrate-observations/2026-04-XX-rag-selection.md`

**N=2 效果决定 L4 候选**：
- 候选 A 有效 → 写进 CLAUDE.md 项目级规则
- 候选 A 无效 / 显式调用更好 → 走候选 B
- 反射信息总是塞不进上下文 → 走候选 C

## Patch-2 · 协同模式分层（v1 已落地）

**主文档**：`~/Vault/知识库/方案模板/协同模式分层-v1.md`

**要点摘录**：

| 模式 | 用户信号 | 关键行为差异 |
|---|---|---|
| 实施 | 改/做/写/修 | 精准执行，小步提交 |
| 调研 | 研究/对比/有哪些 | 多 agent + 证据 |
| 决策 | 选哪个/A 还是 B | 选型矩阵，用户拍板 |
| 元层 | 思路/抽象/底座 | 高抽象，**不动手** |
| 学习 | 什么是/为什么 | 类比，递进讲解 |
| 纯对话 | 感叹/吐槽 | 陪伴，不拉任务 |

候选 4 种（规划/审阅/复盘/情绪支持）暂备选。

**为什么直接落 v1 而非 placeholder**：
- 错了改一张表 5 分钟（可迭代产物）
- 不落下来热度散了就再也写不清楚
- 判据：**错了不会污染别的**

## 本补丁不做的 3 件事

- ❌ 不改 `2026-04-18-collab-substrate-v0.2-parked.md`（冷冻档案保持冷冻）
- ❌ 不写 v0.3 骨架（等 N=4 回看）
- ❌ 不改 CLAUDE.md 加"开工反射"规则（Patch-1 还是 placeholder）

## 本补丁要做的 4 件事（已完成）

- ✅ 建 `~/Vault/知识库/方案模板/选型元底座-目的与原则.md`
- ✅ 建 `~/Vault/知识库/方案模板/协同模式分层-v1.md`
- ✅ 写本补丁文档（L4 placeholder + 模式分层 v1 指向）
- ⏳ 更新 SkillNav MEMORY.md（2 reference + 1 feedback，本补丁之后立即执行）

## 下次对话必做（激活本补丁）

**N=2 实测任务**：RAG 知识库选型调研（家庭/个人/企业/多用户 4 场景的底盘对比矩阵）

**开工前硬性动作**（模拟 L4 反射）：
1. 声明这是 N=2 实测
2. 手动跑一遍"资产盘点"：`_选型元底座-目的与原则.md` §6 里 7 块资产逐一翻
3. 手动识别对话模式：按 `_协同模式分层-v1.md` 判断是**调研 + 决策混合**
4. 按调研 SOP 启动多 agent 并行

**收工后必做**：写 observation
- 路径：`docs/research/collab-substrate-observations/2026-04-XX-rag-selection.md`
- 回答：L4 反射有效吗？6 种模式够用吗？哪条原则被引用？

## 关联文档

- `./2026-04-18-collab-substrate-v0.2-parked.md` — v0.2 冷冻档案（不改）
- `~/Vault/知识库/方案模板/选型元底座-目的与原则.md` — L2 方法论（新建）
- `~/Vault/知识库/方案模板/协同模式分层-v1.md` — L2 模式分层（新建）
- `~/Vault/知识库/_元认知.md` — L0 元认知（上级原则）
- MEMORY.md Active Meta-Skill Work — 当前进度追踪
