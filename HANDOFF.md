# Handoff — Pipeline 自救 + 4/18 论文翻译发布
<!-- Generated by /checkpoint at 2026-04-18 -->

## Objective
- 修复 sync-articles 连续 7 次 CI 失败（Gemini quota 耗尽 + DeepSeek timeout 双挂）
- 完成 2026-04-18 论文雷达翻译并发布

## Completed

### LLM Fallback 切换（commits 1cdcad1 + 59afe60）
- 4 个 workflow 的 `LLM_FALLBACK_PROVIDER` 从 `gemini` → `gpt`（gmn 代理）
  - `.github/workflows/sync-articles.yml`
  - `.github/workflows/generate-daily.yml`
  - `.github/workflows/generate-weekly.yml`
  - `.github/workflows/scrape-community-signals.yml`
- `scripts/lib/llm.mjs:56` GPT provider `reasoning.effort` 从 `xhigh` → `low`（xhigh 经常超过 120s 内部超时）
- GitHub secret `GPT_API_KEY` 已更新为新 key
- 本地 `.env.local` 已加 `GPT_API_KEY`（用户自加）

### 论文翻译与发布
- 4/17 雷达：10 篇全部翻译入库（auto-translate-radar 跑了一轮 + 单篇补跑两轮）
- 4/18 雷达：5/6 翻译成功（HY-World 2.0 第 4 次失败，老问题）
- 5 篇 paper-* 已通过 SQL 直接 update status='published' + published_at=now

### 本地恢复
- 早上 `npm install` 恢复 node_modules（不知何时被清空导致 launchd paper-radar 失败）

## In Progress
- 无

## Next Actions

1. **修 HY-World 2.0 老问题**：`scripts/translate-paper.mjs` 的 ar5iv chunk 拆分逻辑——某些章节超过 30k 字时单 chunk 会塞爆 LLM。
   - 位置：`translate-paper.mjs` 的 chunk 合并逻辑（搜 `Merged ... sections → ... chunks`）
   - 思路：硬上限单 chunk ≤ 15000 字，超过强制再拆

2. **修 sync-articles 误报 failure**：当前 `Failed > 0 → exit 1`，但实际每次 5-15 篇入库。
   - 位置：`scripts/sync-articles.mjs` 末尾的 exit code 判定
   - 思路：改成 `Inserted == 0 && Failed > 0` 才 exit 1，或 `Failed / (Inserted + Failed) > 0.7`

3. **修 slug 冲突**：Google "Gemini 3.1 Flash XXX" 系列 4 篇 slug 截断后碰撞。
   - 位置：`scripts/sync-articles.mjs` 的 slug 生成函数
   - 思路：冲突时加 `-2/-3` 后缀，或检测冲突时先查 DB

4. **S2 API key**：申请已提交，等回复（通常 1-4 周）。Key 到手后塞到 `S2_API_KEY` env 即可。

5. **轮换 gmn GPT key**：会话里贴过的那把 key 安全考虑要换。换完 `gh secret set GPT_API_KEY --body "<new>"`。

## Risks & Decisions

- **DeepSeek 在 GH Actions 上持续 timeout**：基础设施问题（GH 美国节点 → DeepSeek 中国 API），改代码改不掉。当前靠 fallback 接住。
- **gmn 代理偶发 503/429**：上游账号池负载。两边都挂的概率低但存在，长期可考虑加 Anthropic Claude 作三级 fallback。
- **HY-World 2.0**：已第 4 次失败，决定停止盲目重试，需结构性修 chunk 逻辑。

## Verify

- `gh run list --workflow="Sync Articles" --limit 3` — 确认下次跑结果
- `gh secret list | grep GPT_API_KEY` — 确认 secret 时间戳
- `node scripts/auto-translate-radar.mjs --dry-run` — 列出待翻译论文
- Supabase 查询 — 确认论文 published 状态

## Modified Files

本会话已 commit（无未提交代码改动）：
- `.github/workflows/sync-articles.yml`（commit 1cdcad1）
- `.github/workflows/generate-daily.yml`（commit 1cdcad1）
- `.github/workflows/generate-weekly.yml`（commit 1cdcad1）
- `.github/workflows/scrape-community-signals.yml`（commit 1cdcad1）
- `scripts/lib/llm.mjs`（commit 59afe60）

外部副作用：
- `~/Vault/知识库/AI/论文雷达/2026-04-18.md` — 10 个 [ ] → [x]
- `~/Vault/知识库/AI/论文/` — 11 个新论文 markdown（4/17 + 4/18）
- Supabase `articles` 表 — 11 篇 paper-* 入库 + 5 篇 status published
- GitHub secret `GPT_API_KEY` — 更新

工作区残留（**与本次任务无关**，不要 commit）：
- `src/app/globals.css`、`src/app/layout.tsx`、`src/lib/fonts.ts`（你之前的字体调整）
- 大量 png/zip/未追踪文件（历史会话遗留）
